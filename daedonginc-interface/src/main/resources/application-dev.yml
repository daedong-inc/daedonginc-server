spring:
  config:
    activate:
      on-profile: "dev"
  h2:
    console:
      enabled: false
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true