spring:
  config:
    activate:
      on-profile: "s3"

cloud:
  aws:
    s3:
      bucket: daedonginc-bucket
    credentials:
      access-key: ${S3_ACCESS_KEY}
      secret-key: ${S3_SECRET_KEY}
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false